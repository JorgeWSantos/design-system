import {
  colors,
  fonts,
  fontSizes,
  fontWeights,
  lineHeights,
  radii,
  space,
  breakpoints,
  breakpointsPx,
} from '@abqm-ds/tokens';

export const theme = {
  colors,
  fonts,
  fontSizes,
  fontWeights,
  lineHeights,
  radii,
  space,
  breakpoints,
  breakpointsPx,
};

export * from './global';
