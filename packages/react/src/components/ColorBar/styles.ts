import { styled } from 'styled-components';

export const BarWrapper = styled.div`
  display: flex;
  flex-direction: row;
  width: 100%;
  height: 2px;
`

export const ColorSegment = styled.div`
  flex-grow: 1;
`